# Responsibility matrix

This matrix is the completeness ledger for the modern Pure Swift security
responsibility surface. BoringSSL is used only as a responsibility inventory;
completeness means that no selected modern responsibility is unowned. It does
not mean BoringSSL API, ABI, source, internal-implementation, or performance
compatibility.

The matrix records `swift-ssl` mechanism ownership. Public Stream TLS, DTLS, and
QUIC TLS session contracts belong to `swift-tls`; `swift-tls` delegates their
cryptographic and protocol transitions to this package. QUIC CRYPTO
offsets/reassembly belong to `swift-quic`, and WebRTC security generation,
fingerprints, ICE, SRTP, and SCTP belong to `swift-webrtc`. See
[Secure Transport Architecture](../../SECURE_TRANSPORT_ARCHITECTURE.md).

## 1. C-oriented mechanisms and Swift replacements

| BoringSSL mechanism | `swift-ssl` owner | Decision |
|---|---|---|
| `CBS` borrowed parser input | `SSLCore` span-based cursors | Replace with scoped `Span` and checked ranges |
| `CBB` output construction | `SSLCore` bounded builder | Replace with one contiguous owner and caller-buffer output where sized |
| `CRYPTO_BUFFER` / pools | `SSLASN1` and `SSLX509` immutable DER owners | Replace with owner plus ranges; no process-global pool requirement |
| `EVP` algorithm dispatch | `SSLCrypto` narrow protocols and closed algorithm identifiers | Replace; no numeric NID or generic control channel |
| `BIO` | Application transport and explicit byte owners | Exclude; protocol engines perform no I/O |
| `SSL_CTX` / mutable callback configuration | Immutable configuration values and capability requests | Replace; no reentrant callback state |
| `ERR` queue | Typed errors and protocol alert/disposition values | Replace; no thread-local queue |
| `ex_data` | Explicit generic application context outside core state | Replace; no untyped indexed slots |
| reference counts and `get0/get1/set0/set1` | Swift ownership, borrowing, consuming, and noncopyable types | Replace; ownership is visible in type signatures |
| thread callbacks and platform locks | `Synchronization.Mutex<State>` or actor | Replace with one cross-target isolation contract |
| public mutable `BIGNUM` | Fixed-width internal limb types per algorithm | No general public mutable big-number API |
| global RAND behavior | Injected `EntropySource` and explicit DRBG owners | Replace; no hidden fallback or abort-only contract |

## 2. Cryptographic responsibility

Status values are `required`, `policy-gated`, `experimental`, `later`, and `excluded`.

| Family | Algorithms/constructions | Status | Owner and notes |
|---|---|---|---|
| Hash | SHA-256, SHA-384, SHA-512 | Required | `SSLCrypto`; callable implementations are integrated with HMAC/HKDF, TLS 1.3, and supported certificate-signature verification |
| SHA-3/XOF | SHA3-256/512, SHAKE128/256 | Required | `SSLCrypto`; FIPS 202 sponge and incremental contexts are implemented; PQ and protocol integration remain separate responsibilities |
| Additional hash | BLAKE2 variants still justified by modern callers | Later | Separate from TLS completion; no BoringSSL symbol compatibility |
| MAC | HMAC-SHA-256/384/512 | Required | `SSLCrypto` and façade implementations with constant-time tag verification |
| KDF | HKDF-SHA-256/384/512 | Required | Extract/expand implementations; TLS and HPKE labels remain separate constructions |
| AEAD | AES-128-GCM, AES-256-GCM, ChaCha20-Poly1305 | Required | AES-GCM and ChaCha20-Poly1305 callable implementations protect all three TLS 1.3 cipher suites; open failure exposes no plaintext |
| Classical KEX | X25519 and P-256 | Required | `SSLCrypto` owns both fixed-width key-agreement backends; P-256 implements HPKE KEM `0x0010` and the TLS 1.3 `secp256r1` key-share group through distinct role-specific owners |
| Classical signature | Ed25519 | Required | `Ed25519` conforms to separate message-signing and verification protocols with noncopyable private and owned validated public keys |
| Certificate signature verification | ECDSA P-256/384/521; RSA-PSS SHA-256/384/512 | Policy-gated | X.509 verification consumes public inputs only; separate noncopyable P-256 and RSA private-key owners are restricted to the explicit TLS signing profile in ADR 0051 |
| Certificate compatibility verification | RSA PKCS #1 v1.5 with approved SHA-2 hashes | Policy-gated | `RSAPKCS1v15` implements verification-only SHA-256/384/512 with strict canonical DigestInfo and X.509 integration for deployed PKI; it is never a TLS CertificateVerify signer |
| ML-KEM | ML-KEM-768 and ML-KEM-1024 | Required | FIPS 203 semantics; implicit rejection for correctly sized ciphertext |
| ML-DSA | ML-DSA-44/65/87 | Required primitive | FIPS 204; TLS/X.509 use remains wire-policy gated |
| TLS hybrid | X25519MLKEM768 pinned to `draft-ietf-tls-ecdhe-mlkem-05` | Experimental until standardized | Callable role-specific key exchange is integrated with Stream TLS and QUIC under an explicit revision policy; draft Kyber groups are excluded |
| HPKE | RFC 9180 X25519 and P-256 DHKEM Base, PSK, Auth, and AuthPSK modes with HKDF-SHA256/384/512 and AES-GCM/ChaCha20-Poly1305 | Required | X25519 passes its current timing and memory gates. P-256 KEM `0x0010` is callable with separate key owners, caller-owned DH output, typed failures, the shared noncopyable context contract, RFC A.3 Base evidence, all-mode tests, and final Native/WASI/Embedded execution; its sanitizer, differential, memory, constant-time automation, and performance gates remain open |
| PQ HPKE/X-Wing | Standardized ML-KEM suites and X-Wing draft profile | Experimental | X-Wing KEM is not the TLS X25519MLKEM concatenation construction |
| SLH-DSA | FIPS 205 parameter sets | Later | Primitive surface; no pretend TLS/X.509 integration |
| Entropy/DRBG | Explicit entropy source and reseed-capable DRBG | Required | `HMACDRBG` implements SP 800-90A-style HMAC-SHA-256 state with injected `EntropySource`; process-fork detection and platform health adapters remain external |
| Constant-time utilities | equality, conditional selection, fixed-width arithmetic helpers | Required | Internal/public boundary carefully limited |

## 3. Formats and PKI responsibility

| Responsibility | Status | Contract |
|---|---|---|
| Strict DER TLV and primitive codecs | Required | Canonical encoding, complete consumption, checked arithmetic, explicit budgets |
| RFC 7468 PEM | Required | Known labels, strict Base64, decoded DER owner; malformed blocks are typed failures |
| BER-to-DER repair | Excluded | No silent compatibility normalization |
| SPKI and PKCS #8 | Required | Strict structural parsers own immutable DER plus checked key ranges; RFC 5915 ECPrivateKey decoding validates version, curve parameters, scalar-sized encoding, optional public key, and PKCS #8 outer/inner curve agreement as a format responsibility; it is not a TLS signing-key factory |
| Encrypted PKCS #8 | Required | `EncryptedPrivateKeyInfo` strictly parses PBES2/PBKDF2-HMAC-SHA256/AES-256-GCM and rejects SHA-1/CBC/unknown profiles; `PBES2AES256GCM` consumes borrowed password bytes, enforces iteration policy before decryption, authenticates before returning plaintext, and retains private-key DER only in wipe-on-destroy owners |
| PKCS #12 | Required modern subset | `PKCS12Archive` owns a strict PFX v3 container with one PBES2/PBKDF2-HMAC-SHA256/AES-256-GCM encrypted key and an ordered certificate chain; `PKCS12IdentityCoding` separates cryptography from storage, and the Ed25519 codec verifies leaf/private-key correspondence before sealing and after opening; legacy PBE, bag attributes, and MacData are rejected |
| CMS/PKCS #7 certificate containers | Required subset | `CMSCertificateCollection` parses and emits canonical certificate-only SignedData, owns one DER buffer with borrowed certificate ranges, and rejects content, CRL, certificate-choice, digest, and signer responsibilities outside this subset |
| X.509 certificate and extension parse | Required | Immutable DER owner plus ranges; syntax validity is not path validity |
| RFC 5280 path building and validation | Required | `X509PathValidator` performs bounded unordered search, loop detection, issuer/subject DER-name matching, validity windows, trust-anchor selection, BasicConstraints CA/pathLen, optional keyCertSign, and signature/algorithm policy; revocation and advanced policy remain separate |
| Service identity | Required | SAN-only matching; no Common Name fallback; `X509Certificate.matchesDNSName` implements ASCII DNS labels and one-label left-most wildcards |
| Trust store | Required protocol | Caller-supplied immutable trust records; platform adapters are separate |
| CRL and OCSP | Required | Acquisition, evidence validation, and hard/soft-fail policy separated |
| Certificate Transparency inputs | Required | SCT parsing and verification capability; log-list acquisition is external |
| Delegated credentials | Required | TLS policy-gated modern extension |
| Raw public keys | Required | Explicit authentication profile, never confused with X.509 trust |
| Certificate compression | Required | Bounded decompression and transcript-correct encoding |

## 4. Secure transport responsibility

| Responsibility | Status | Boundary |
|---|---|---|
| TLS 1.3 stream | Required | `TLS13ClientHandshake` and `TLS13ServerHandshake` adapt TLS records to the shared record-independent core; the adapter owns framing, AEAD protectors, application records, encrypted post-handshake tickets, and KeyUpdate record transitions, while the core owns transcript, authentication, key schedule, Finished, resumption, and traffic-secret evolution |
| TLS 1.3 client authentication | Required | `TLS13ClientIdentity` owns the client's certificate chain and noncopyable signer; `TLS13ClientAuthenticationConfiguration` owns main/post/both timing, optional/required policy, and injected `TLS13ClientCertificateValidating` capability; the core owns main-handshake and independent post-handshake CertificateRequest/Certificate/CertificateVerify/Finished transcript transitions plus typed external credential, signing, and trust suspension; Stream and DTLS own application-epoch framing, while QUIC intentionally has no PHA API under RFC 9001 |
| TLS resumption | Required | Application traffic/exporter secrets are derived after Server Finished while a separate noncopyable resumption master owner is derived only after Client Finished; ticket issue returns encrypted wire output plus server-side single-use state; client receipt derives matching state; PSK ClientHello/binder integration, ticket-age validation, and resumed transcript integration are implemented; cross-process replay coordination and persistence remain external policy |
| TLS 0-RTT | Required | Separate from resumption; explicit replay policy; no automatic replay |
| ECH RFC 9849 | Required | Strict config-list parsing/selection, immutable X25519 key snapshots, HPKE ClientHelloInner/Outer sealing/opening, padding, acceptance confirmation, authenticated reject/retry outcome, HelloRetryRequest continuation with HPKE-context reuse and empty second encapsulation, PSK binder recomputation, and Stream/DTLS/QUIC core integration are callable; input encapsulation/ciphertext remain borrowed and output is sealed directly into its final owner; DNS, retry transport, and key rotation acquisition are external |
| DTLS 1.3 | Required | Epoch/sequence/replay, fragmentation/reassembly, protected records, ACK generation/processing, flight and timer state, fixed CID, application data, KeyUpdate, and mandatory peer-bound authenticated Cookie/HelloRetryRequest are callable; the transport owns datagram I/O, canonical peer identity, timer delivery, path migration policy, and PMTU decisions |
| DTLS-SRTP | Required | The client/server configurations own offered/selected protection profiles and MKI echo policy; the DTLS core owns use_srtp negotiation and exporter-derived keying material; media transport remains external |
| QUIC RFC 9001 | Required | TLS handshake bytes and secret events only; `QUICInitialSecrets` implements RFC 9001 v1 Initial HKDF derivation, and `QUICTLSClientHandshake` / `QUICTLSServerHandshake` connect ordered zero-copy message borrows to record-independent TLS state machines and directional secret effects; current offset/reassembly types move to `swift-quic` before migration completion |
| ALPN, SNI, record size limit | Required | Typed extension configuration and negotiated results |
| Key logging | Debug-only policy | Explicit diagnostic capability, unavailable by default, secrets never reach general logs |
| SSL/TLS 1.0-1.2 | Excluded | No downgrade or compatibility backend |
| DTLS 1.0 | Excluded | No old record/handshake profile |
| DTLS 1.2 WebRTC profile | Implemented as a scoped sans-I/O mechanism | `SSLDTLS` owns `use_srtp`, `extended_master_secret`, `renegotiation_info`, ECDHE-ECDSA handshake, cookie/address validation, bounded fragments, anti-replay, flights/retransmission state, AES-GCM records, and the RFC 5705/5764 SRTP exporter; `swift-tls` supplies identity and transport/timer adapters; no general TLS 1.2 fallback |
| Renegotiation, False Start, NPN, Channel ID | Excluded | Historical features outside the modern profile |

## 5. Explicit legacy algorithm exclusions

The implementation does not contain negotiation or compatibility paths for MD4, MD5, MD5-SHA1, RC4, DES, 3DES, Blowfish, CAST, TLS CBC suites, RSA key transport, finite-field DH, DSA, ECDSA-SHA1, RSA PKCS #1 v1.5 TLS handshake signing, or `X25519Kyber768Draft00`.

Unknown, excluded, and experimental-disabled algorithms remain distinguishable typed policy failures.

## 6. Draft and standards policy

- Final standards receive stable public identifiers.
- Draft algorithms expose an experimental policy, and their exact revision is pinned by an ADR and package release. A public wire name registered without a draft suffix may retain that name, but its semantics are never silently reinterpreted.
- Draft wire identifiers are not silently reinterpreted after an update.
- A draft update requires vector refresh, interoperability evidence, migration notes, and an explicit decision record.
- FIPS algorithm conformance does not imply that this library is a FIPS-validated module.

## 7. Completion ledger

The implementation status is tracked by responsibility rather than by header count. A row changes to complete only after the gates in `VERIFICATION.md` pass. Missing APIs remain documentation backlog until a callable implementation exists; callable partial paths must fail explicitly and carry the required incomplete-implementation marker.

### 7.1 Current implementation snapshot

No row below is complete unless the evidence column explicitly says that every completion gate passed.

| Responsibility | Production implementation now | Evidence now | Remaining before complete |
|---|---|---|---|
| Core byte and secret foundation | Owned contiguous bytes, scoped spans, checked cursor/builder/ranges, parsing budgets, noncopyable secret allocation and wipe | Native unit tests, lifetime-negative fixtures, Native/WASI/Embedded WASI target-validation execution, ASan/TSan/UBSan, optimized wipe inspection | Measured copy/allocation budgets and archived release-gate logs |
| SHA-256 | Real incremental and one-shot implementation with scalar and ARM64 kernels and caller-provided output | Known-answer/boundary tests, million-byte vector, scalar/ARM64 comparison, Native/WASI/Embedded WASI target-validation execution, sanitizer coverage | Committed independent differential evidence and formal clean benchmark |
| HMAC-SHA-256 | Real incremental and one-shot keyed implementation with noncopyable context, stable secret-state storage, caller-provided output, and constant-time verification | RFC 4231 cases 1/2/3/4/6/7, incremental and failure-contract tests, Native/WASI/Embedded WASI façade execution, ASan/TSan/UBSan, `-O`/`-Osize` in-place wipe inspection | Committed independent differential evidence, measured allocation/copy counts, formal clean benchmark, and security review |
| HKDF-SHA-256 | Real extract and expand implementation with borrowed inputs, caller-provided output, one-time prepared HMAC schedule, direct full-block writes, and typed exact-length/overlap failures | RFC 5869 SHA-256 cases 1/2/3, additional fixed fixtures, maximum-length and overlap boundaries, Native/WASI/Embedded WASI execution, explicit façade execution, ASan/TSan/UBSan, and six `-O`/`-Osize` code-generation gates for heap freedom, direct output, and scoped erasure | Independent committed differential evidence, measured allocation/copy counts, formal benchmark, and security review |
| AES-GCM | AES-128/192/256 prepared key schedules, AES block encryption, GCTR, GHASH, caller-owned seal/open spans, pre-decryption constant-time tag validation, ARM64 SIMD counter advance, reversed powers, and four/eight-block GHASH aggregation | NIST empty-message, single-block, and AAD vectors; 1,000-case differential checks for both batched GHASH widths; round-trip, authentication-failure output preservation, and partial-overlap rejection; Native/WASI/Embedded runtime validation; 165-test ASan/UBSan passes | Independent external differential corpus, boundary/limit tests, automated code-generation review, measured allocation/copy counts, formal benchmark, TSan on a working pinned toolchain, and security review |
| X25519 | RFC 7748 Montgomery ladder, clamped scalar handling, noncopyable private/shared-secret owners, caller-owned in-place outputs, fixed-base table multiplication, scoped public-key borrows, and transactional all-zero shared-secret rejection | RFC 7748 and independent deterministic vectors; invalid-length and all-zero-peer output-preservation failures; Hybrid Native/WASI/Embedded execution and focused ASan; committed formal zero-allocation/zero-dynamic-copy evidence for caller-owned public/shared outputs | Broader independent differential and malformed-coordinate corpora, automated constant-time code-generation gate, and security review |
| TLS `secp256r1` key exchange | Role-specific noncopyable client/server key-exchange owners over the fixed-control-flow P-256 backend, strict 65-byte uncompressed peer points, borrowed inbound shares, one-shot scalar consumption, and one 32-byte final shared-secret owner | Matching-secret, malformed-point, and second-use failure tests; full TLS Stream, QUIC CRYPTO, and DTLS Cookie/retry handshakes; final Native/WASI/Embedded WASI Release build/link/run | Independent TLS interoperability, focused sanitizer and automated constant-time route coverage, formal allocation/copy evidence, paired benchmark, and security review |
| HPKE X25519 | RFC 9180 Base/PSK/Auth/AuthPSK, all SHA-2 KDF and required AEAD combinations, stack-scoped DH output, prepared AEAD ownership, sequence nonces, exporter, and caller-owned in-place payload operations | RFC A.2 vectors for every mode; all nine KDF/AEAD combinations through two sequential messages; Native/WASI/Embedded execution of all three AEADs through sequence two; 165-test ASan/UBSan passes; committed formal memory evidence with a zero payload slope; and a committed 30-pair timing artifact whose four workloads pass the `1.10x` lower-confidence-bound gate | Independent differential interoperability, automated constant-time review, TSan on a working pinned toolchain, and security review |
| HPKE P-256 | RFC 9180 KEM `0x0010` Base/PSK/Auth/AuthPSK over noncopyable P-256 scalar/shared-secret owners, immutable public precomputation, caller-owned ECDH output, and the shared HPKE KDF/AEAD context contract | RFC A.3 AES-128-GCM Base vector, all-mode Native round trips, independent field and ECDH fixtures, typed invalid scalar/point/encapsulation failures, and final Native/WASI/Embedded target-validation execution | Differential interoperability, ASan/UBSan, memory artifact, automated constant-time review, formal paired timing artifact, `1.10x` performance gate, and security review; exploratory ARM64 sender and recipient setup measurements currently lose to pinned BoringSSL |
| Ed25519 | Noncopyable private seed, owned validated public key, separate signing/verification protocols, fixed-iteration scalar reduction, and mask-selected secret scalar multiplication | RFC 8032 signing/verification, modified-message and noncanonical-scalar failures, Native/WASI/Embedded façade verification, Native TLS/Core tests, dedicated three-target QUIC/TLS authentication, focused ASan over 11 tests, and manual optimized ARM64 control-flow inspection | Broader official corpus, automated constant-time code-generation gate, TSan/UBSan, allocation/copy measurement, interoperability, benchmark, and security review |
| NIST ECDSA | P-256/P-384/P-521 deterministic RFC 6979 signing plus fixed-width verification; each curve uses a noncopyable wiped scalar owner and fixed-control-flow secret multiplication | Independent raw verification vectors and mutation failures, P-384/P-521 generator/key-agreement/signing round trips, RFC 6979 P-256 signing vector, real X.509 fixtures, TLS P-256 server/client authentication, PKCS #8 import failures, and Native/WASI/Embedded Release signing/verification routes | Broader differential corpus, sanitizer and automated constant-time coverage, allocation/copy measurement, benchmark, interoperability, and security review |
| HMAC-DRBG | SP 800-90A-style instantiate, generate, additional-input update, reseed, request limit, and noncopyable `SecretBytes` state | Independent deterministic vectors, request-limit failure before mutation, Native/WASI/Embedded WASI target validation | Entropy fault/reseed corpus, sanitizer/code-generation review, measured allocation/copy counts, formal benchmark, and security review |
| SHA3/SHAKE | FIPS 202 Keccak-f[1600] sponge with incremental SHA3-256/512 and SHAKE128/256 contexts, scoped output, and strict length checks | FIPS 202 known answers, clone equivalence, output-preservation failure test, Native/WASI/Embedded WASI target validation | Independent differential corpus, long-message boundary corpus, sanitizer/code-generation review, formal benchmark, and security review |
| ML-KEM-768/1024 | Callable FIPS 203 KEM and in-place KEM implementations with noncopyable private/shared-secret owners, immutable expanded keys, implicit rejection, SIMD NTT, and two-stream Keccak | NIST ACVP/TR1 fixtures, arithmetic differential/boundary tests, typed no-write failures, focused ASan, six optimized secure-wipe code-generation configurations, Native/WASI/Embedded WASI façade execution, pinned-BoringSSL bidirectional interoperability, a committed formal 30-pair benchmark whose six workloads pass the `1.10x` lower-confidence-bound gate, and a committed linear allocation/dynamic-copy artifact for all six paths | Broader external corpus, automated constant-time review, TSan/UBSan availability statement, and security review |
| ML-DSA-44/65/87 | Callable FIPS 204 contextual randomized and in-place signature implementations over one parameterized core, with distinct public key types, noncopyable private-key owners, immutable expanded secret coefficients, synchronized immutable public expansion, scoped raw signing workspaces, SIMD NTT, and two-stream Keccak | NIST ACVP key-generation and Wycheproof seeded-signature fixtures for all parameter sets; success/failure/domain-separation tests; Native/WASI/Embedded WASI façade execution; ASan façade execution; pinned-BoringSSL bidirectional signature interoperability and mutation rejection; committed formal allocation/dynamic-copy evidence for all nine paths; and a committed formal 30-pair timing artifact whose nine workloads exceed the `1.10x` lower-confidence-bound target | Broader external corpus, automated constant-time review, TSan/UBSan availability statement, and security review |
| X25519MLKEM768 TLS key exchange | Role-specific one-shot client/server protocols, exact draft-05 share ordering and lengths, ML-KEM encapsulation-key validation and implicit rejection, X25519 all-zero rejection, noncopyable private/secret owners, borrowed received shares, and direct final-owner output | Native key-exchange/Core/Stream/QUIC success and failure tests; Native/WASI/Embedded dedicated runtime execution; dedicated Hybrid ASan execution; committed formal bidirectional pinned-BoringSSL interoperability and 30-pair timing artifact passing the `1.10x` lower-confidence-bound gate; committed formal steady-state allocation/dynamic-copy artifact with exact linear budgets | Broader corpus, a second independent peer, automated constant-time review, and security review |
| Remaining cryptography | Additional approved hybrid constructions remain outside the callable implementation; ML-DSA-44/65/87, ML-KEM, X25519MLKEM768, both HPKE KEMs, all four NIST classical key-agreement families, P-256/P-384/P-521 ECDSA signing, and RSA-PSS SHA-256 signing are callable; RSA-PSS SHA-384/512 and RSA PKCS #1 v1.5 SHA-2 remain verification-only | Native vectors cover all FIPS 204 ML-DSA parameter sets, FIPS 203, both RFC 9180 KEM profiles, all NIST curve operations, P-256 signing, and RSA-PSS signing; production P-256/RSA TLS authentication and PKCS #8 import execute on Native/WASI/Embedded; the verification-only constructions retain independent vectors, real X.509 routes, sanitizer evidence where available, and three-target runtime evidence | Additional approved hybrid constructions, broader signing interoperability and differential corpora, allocation/copy and performance artifacts, automated constant-time gates, and security review |
| ASN.1 | Strict DER cursor/TLV, canonical primitive codecs including OID emission, bounded writer, strict RFC 7468 PEM codec, SPKI, unencrypted PKCS #8, RFC 5915 ECPrivateKey decoder, modern PKCS #12 identity archives, and certificate-only CMS SignedData | Native unit tests for cursor, primitive values, canonical OID encoding, transactional writer failures, canonical Base64, CRLF, malformed input, output limits, RFC 5915 P-256 parsing, curve mismatch, PKCS #12 identity behavior, and CMS canonical certificate sets | Broader malformed corpora, fuzzing, interoperability, and full target evidence |
| Encrypted PKCS #8 | Strict PBES2/PBKDF2-HMAC-SHA256/AES-256-GCM parser, encoder, password profile, policy-bounded iteration count, authenticated decryption, scoped password borrows, stack-wiped derived key, and wipe-on-destroy plaintext/private-key owners | Native round-trip, wrong-password, ciphertext-modification, unsupported-algorithm, and iteration-policy tests; PBKDF2 independent known-answer tests | Independent OpenSSL/BoringSSL interoperability, malformed DER corpus, Native/WASI/Embedded execution, sanitizer evidence, allocation/copy measurement, and security review |
| PKCS #12 and CMS certificate interchange | Strict PFX v3 parser/encoder for one modern encrypted Ed25519 identity plus ordered X.509 chain, noncopyable decrypted owner, constant-time leaf/key match, and strict certificate-only CMS SignedData parser/encoder | Native PKCS #12 seal/parse/open and CMS encode/parse tests cover successful borrows plus password, authentication, key mismatch, unsupported algorithm/content, canonical ordering, MacData, empty collection, and range failures | Independent OpenSSL/BoringSSL interoperability, malformed corpus, Native/WASI/Embedded execution, sanitizer evidence, allocation/copy measurement, and security review |
| X.509 | Certificate/TBSCertificate/Validity/signature/SPKI structural parser, issuer-key signature verification, Ed25519, verification-only ECDSA P-256/P-384/P-521, RSA-PSS-with-SHA2, and RSA-PKCS1-v1_5-with-SHA2 signatures, strict v3 extension parsing, SPKI, unencrypted PKCS #8 owners, and bounded trust-anchor path validation | Native X509 tests for certificate ranges/time fields, Ed25519, ECDSA P-256/P-384/P-521, RSA-PSS, and RSA PKCS #1 v1.5 SHA-2 signatures; modified-signature failures; RSA key-type mismatch; real leaf/root path validation with SAN; duplicate extensions; X25519 SPKI; and PKCS #8 version failures; RSA public verification has ASan/UBSan and Native/WASI/Embedded runtime evidence, while TSan is blocked by a pinned-toolchain compiler assertion | Revocation, advanced RFC 5280 policy, broader differential corpus, target evidence, and interoperability |
| TLS/DTLS | One record-independent TLS 1.3 semantic core shared by Stream, DTLS, and QUIC, narrow client/server protocols, HKDF key schedule, distinct directional 0-RTT, Server-Finished application, and Client-Finished resumption-secret owners, explicit early-data client/server limits and replay-policy injection, AEAD Stream and DTLS record adapters, synchronous X25519, `secp256r1`, or X25519MLKEM768 key exchange with Ed25519, P-256 ECDSA, or RSA-PSS server/client authentication for all three TLS 1.3 AEAD suites, optional/required main/post/both client-certificate timing, local and external credential/trust capabilities, certificate compression, delegated credentials, raw public keys, strict Ed25519/P-256/RSA PKCS #8 signing-key import, paired encrypted NewSessionTicket/server-state issuance, PSK extension/binder primitives, server ticket-age validation, single-use PSK resumption, RFC 9849 ECH accept/reject and HelloRetryRequest continuation, post-handshake KeyUpdate, SAN-only server identity helper, bounded DTLS replay windows, and DTLS-SRTP negotiation/export; Stream and QUIC support 0-RTT while DTLS rejects it | RFC 8448 handshake/application/exporter/resumption and ticket-PSK known answers; Native classical, P-256, Hybrid, ECH, PSK, and 0-RTT Stream/Core/QUIC completion; early-data accept, policy reject, unknown-ticket discard, byte-limit enforcement, HRR removal, Stream EndOfEarlyData, QUIC directional secret, and ticket-extension tests; Ed25519/P-256/RSA main-handshake authentication plus RFC 8446 post-handshake success, optional empty Certificate, external credential/signature/trust suspension, duplicate-context rejection, tampered Finished rejection, authenticated-identity timing, and Stream/DTLS application-epoch integration; accepted/rejected ECH HelloRetryRequest, PSK binder recomputation, second-HRR rejection, peer-bound DTLS Cookie, fragmentation, ACK, retransmission, CID, application-data, KeyUpdate, and DTLS-SRTP tests; focused AddressSanitizer; ECH bidirectional pinned-BoringSSL interoperability; final Native/WASI/Embedded WASI Release signing/verification target validation | Broader RFC 8448 corpus, replay-store integration fixtures, dynamic DTLS CID/path/PMTU policy, broader allocation/copy measurement, fuzzing, security review, and full-handshake external interoperability |
| QUIC TLS integration | Profile actions, checked batch, noncopyable traffic-secret event, ordered action/secret step output, RFC 9001 v1 Initial derivation, bounded per-level CRYPTO reassembly, zero-copy message delivery, record-independent full, client-authenticated, resumed, and 0-RTT TLS handshakes, explicit transport-parameter bytes and ALPN selection, role-correct client-write/server-read 0-RTT delivery, and role-correct handshake/1-RTT secret delivery | Native full-handshake including X25519, P-256, and Hybrid key exchange, required-client-authentication, PSK-resumption, 0-RTT directionality and EndOfEarlyData omission, tampered-Finished, wrong-epoch, out-of-order CRYPTO, overlap-conflict, and directional-secret tests; three guarded core/adapter repetitions; focused AddressSanitizer execution; dedicated Native/WASI/Embedded WASI full-handshake runtime validation with directional-secret comparison | QUIC-stack application of negotiated transport parameters and ALPN, packet protection, 0-RTT external interoperability, allocation/copy measurement, fuzzing, and security review |
| Platform adapters | `SystemEntropySource`, `SystemWallClock`, and `SystemMonotonicClock` implement the shared capability protocols with typed host-POSIX and WASI backends | Native clock tests plus Native/WASI/Embedded WASI façade compile, link, and runtime execution | Deployment-specific persistence/trust/transport adapters, clock-failure injection corpus, and release review |
| `SSL` façade | One-import umbrella; explicit primitive adapters; protocol-backed TLS 1.3 client factories for local or external server trust and server factories for external credential/signature capabilities across X25519, `secp256r1`, and X25519MLKEM768 | Primitive success/failure routes plus all client group/trust compositions, all server group credential-suspension routes, entropy/clock failures, and duplicate-credential-type rejection execute through the façade on Native/WASI/Embedded WASI | Allocation/copy evidence for factory composition and P-256 signing, and a reviewed stability surface |
| SHA-256 benchmark | Separate manual harness and BoringSSL comparison driver | Formal 30-pair Native measurement with confidence intervals is recorded in README; the result is retained as independent historical evidence, not as a completion gate | No SHA-256 performance work is required for this migration; any future optimization is a separate benchmark task |

Protocol declarations, model types, compilation, and a single target probe are not evidence that the corresponding cryptographic or protocol responsibility is complete.
