import SwiftSSLCore

/// A caller-supplied cryptographically secure random byte source.
public typealias EntropySource = SwiftSSLCore.EntropySource

/// The target runtime's cryptographically secure random byte source.
public typealias SystemEntropySource = SwiftSSLCore.SystemEntropySource
